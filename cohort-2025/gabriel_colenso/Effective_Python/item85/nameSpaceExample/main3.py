from optimise.utils import view as view_float
from override.utils import view as view_string


value = 50

print('Float representation: ' '%r' % view_float(value))
print('String representation: ' '%r' % view_string(value))

