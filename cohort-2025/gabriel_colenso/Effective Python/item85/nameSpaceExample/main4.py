import optimise.utils
import override.utils


value = 50

print('Float representation: ' '%r' % optimise.utils.view(value))
print('String representation: ' '%r' % override.utils.view(value))

